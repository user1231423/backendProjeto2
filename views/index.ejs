<!DOCTYPE html>
<html>

<head>
    <meta http-equiv="Content-Type" const="text/html;charset=UTF-8" />
    <link href="http://fonts.googleapis.com/css?family=Comfortaa" rel="stylesheet" type="text/css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <script src="/socket.io/socket.io.js"></script>
    <title> Chat App</title>
</head>

<body>
    <header>
        <h1 class="text-center">Socket.io Web Chat</h2>
    </header>

    <div class="container" style="margin-top: 5rem">
        <div class="row">
            <div class="col-sm-4" style="height: 35rem; overflow-y: auto">

                <div class="row" style="margin-bottom: 2rem; margin-top: 0.2rem;">
                    <div class="col-md-8">
                        <input id="new_name" type="text" placeholder="Insert new name..." class="form-control" />
                    </div>
                    <div class="col-md-4">
                        <button id="change_name" class="btn btn-info btn-block">Change</button>
                    </div>
                </div>

                <table class="table  table-striped" style="border-collapse:separate; border-spacing: 0 15px">
                    <tbody id="userList">
                    </tbody>
                </table>

            </div>

            <div class="col-sm-8 scroll">
                <div class="chatbody" id="chatWindow" style="height: 35rem; overflow-y: auto">

                    <table class="table" style="border-collapse:separate; border-spacing: 0 15px">
                        <tbody id="chatroom">
                        </tbody>
                    </table>

                </div>

                <div class="row" id="input_zone">
                    <div class="col-md-8">
                        <input id="message" type="text" placeholder="Insert text..." class="form-control" />
                    </div>
                    <div class="col-md-4">
                        <button id="send_message" class="btn btn-info btn-block">Send</button>
                    </div>
                </div>

            </div>
        </div>
    </div>
    <script src="http://code.jquery.com/jquery-latest.min.js"></script>
    <script src="chat.js"></script>

    <script>
        $(function() {
            $("#send_message").click(function() {
                $('#chatWindow').animate({
                    scrollTop: $('#chatWindow')[0].scrollHeight
                }, "fast");
            });
        });

        $(function() {
            $("#change_name").click(function() {
                $('#chatWindow').animate({
                    scrollTop: $('#chatWindow')[0].scrollHeight
                }, "fast");
            });
        });
    </script>

</html>